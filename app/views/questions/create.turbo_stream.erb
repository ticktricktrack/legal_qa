<%= turbo_stream.append "questions-list" do %>
  <%= render "shared/question", question: @question %>
<% end %>

<%= turbo_stream.replace "#{dom_id(Question.new)}_form" do %>
  <%= render "form", question: Question.new %>
<% end %>
